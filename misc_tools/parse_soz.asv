function [soz_labels,soz_chs] = parse_soz(text,labels,name)

% split up the labels
C = strsplit(text,',');

% clean them
clean_C = decompose_labels(C,name);

% find the indices
[~,soz_chs] = ismember(clean_C,labels);

if any(soz_chs 


end